# Predicting-Customer-Purchase-Behavior-using-K-Nearest-Neighbors
A machine learning classification project to predict customer purchase behavior based on age and estimated salary using the K-Nearest Neighbors (KNN) algorithm. Includes data preprocessing, feature scaling, hyperparameter tuning, model evaluation, and ROC-AUC analysis.
